<div class="main-visual">
  <div class="main-visual-content">
    <p class="main-visual-text">
      あなただけが知るおすすめスポットを、みんなに紹介しよう！<br />
      〜誰も知らない魅力が、今ここに〜<br />
    </p>
    <p class="main-visual-subtitle">
      日本各地のスポット投稿サイト
    </p>
    <p class="main-visual-title">
      JapanSiteInfo
    </p>
    </div>
    <div class="theTarget">
      <div class="theTarget-image1"></div>
      <div class="theTarget-image2"></div>
      <div class="theTarget-image3"></div>
      <div class="theTarget-image4"></div>
      <div class="theTarget-image5"></div>
    </div>
  </div>

<div class="container-xxl mt-5 mx-5">
  <div class="row">
    <div class="mt-5 col-md-8 mr-5" style="margin-left: 0; margin-right: 0; padding-left: 0;">
      <h3>新着スポット</h3>
      <table class="table table-hover table-inverse mt-4">
        <thead>
          <th></th>
          <th>タイトル</th>
          <th>都道府県</th>
          <th>市区町村</th>
          <th>投稿日</th>
          <th>来訪日</th>
          <th>評価</th>
          <th>いいね</th>
        </thead>
        <tbody>
          <% @spots.each do |spot| %>
            <tr>
              <td>
                <%= attachment_image_tag spot, :spot_image1, format: "jpeg", fallback: "noimage-1-760x460.png", size: "100x100" %>
              </td>
              <td>
                <%= link_to spot_path(spot) do %>
                  <%= spot.title %>
                <% end %>
              </td>
              <td><%= spot.prefecture %></td>
              <td><%= spot.city %></td>
              <td><%= spot.created_at.strftime("%Y年%-m月%-d日") %></td>
              <td>
                <% if spot.visited_day.nil? %>
                  不明
                <% else %>
                  <%= spot.visited_day.strftime("%Y年%-m月%-d日") %>
                <% end %>
              </td>
              <% if spot.rate.nil? %>
                <td>無評価</td>
              <% else %>
                <td id="star-rate-<%= spot.id %>"></td>
                <script>
                  $('#star-rate-<%= spot.id %>').raty({
                    size: 36,
                    starOff: '<%= asset_path('star-off.png') %>',
                    starOn: '<%= asset_path('star-on.png') %>',
                    readOnly: true,
                    score: <%= spot.rate %>,
                  });
                </script>
              <% end %>
              <td class="favorite-<%= spot.id %>">
                <%= render "favorites/list_favorite", spot: spot %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @spots, theme: 'twitter-bootstrap-4' %>
    </div>

    <% if current_user.nil? %>
      <div class="mt-5 col-md-3 ml-5" style="margin-left: 0; margin-right: 0; padding-left: 0;">
          <h3>スポットを探してみる</h3>
        <%= search_form_for @q do |f| %>
          <%= f.label "都道府県", class: "mt-4" %><br />
          <%= f.select :prefecture_eq, Spot.prefectures.map{|k, v| [Spot.prefectures_i18n[k], v]}, :include_blank => '指定なし' %><br />

          <%= f.label "市区町村", class: "mt-3" %><br />
          <%= f.search_field :city_cont %><br />

          <%= f.label "タイトル", class: "mt-3" %><br />
          <%= f.search_field :title %><br />

          <%= f.label "キーワード", class: "mt-3" %><br />
          <%= f.search_field :keywords_keyword_cont %><br />

          <%= f.submit "検索", class: "btn btn-secondary mt-3" %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>